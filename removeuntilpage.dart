import 'package:flutter/material.dart';

class RemoveUntilPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Remove Until'),
      ),
      body: Center(
        child: Text('Remove Until'),
      ),
    );
  }
}
