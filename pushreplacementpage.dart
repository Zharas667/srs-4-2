import 'package:flutter/material.dart';

class PushReplacementPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Push Replacement'),
      ),
      body: Center(
        child: Text('Its\ Push Replacement'),
      ),
    );
  }
}
