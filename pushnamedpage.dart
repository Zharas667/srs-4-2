import 'package:flutter/material.dart';

class PushNamedPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Push Named'),
      ),
      body: Center(
        child: Text('Its was Push Named Navigation'),
      ),
    );
  }
}
